# GitHub App real E2E check

This file verifies the local Worker can use a GitHub App installation token.

- issue: #77
- branch: feature/github-app-real-e2e-77b
- checks: selected repo, branch creation, push-files, pull request creation, close keyword linkage