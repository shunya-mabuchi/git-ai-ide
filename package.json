{
  "name": "git-ai-ide",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "dev": "pnpm --filter @git-ai-ide/web dev",
    "dev:worker": "pnpm --filter @git-ai-ide/worker dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
